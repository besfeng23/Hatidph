const screens=[...document.querySelectorAll('.screen')];const nav=document.getElementById('nav');const navRoutes=['home','trips','wallet','safety','account'];function go(id){screens.forEach(s=>s.classList.toggle('active',s.id===id));nav.classList.toggle('visible',navRoutes.includes(id));nav.querySelectorAll('button').forEach(b=>b.classList.toggle('active',b.dataset.go===id));}document.addEventListener('click',e=>{const target=e.target.closest('[data-go]');if(target)go(target.dataset.go);});const phone=document.getElementById('phone');const toOtp=document.getElementById('to-otp');phone?.addEventListener('input',()=>{toOtp.disabled=phone.value.replace(/\D/g,'').length<10});const otp=document.getElementById('otp-code');const toProfile=document.getElementById('to-profile');otp?.addEventListener('input',()=>{toProfile.disabled=otp.value.replace(/\D/g,'').length<6});document.querySelectorAll('.ride').forEach(button=>{button.addEventListener('click',()=>{document.querySelectorAll('.ride').forEach(r=>r.classList.remove('selected'));button.classList.add('selected');document.getElementById('confirm').textContent='Confirm '+button.dataset.ride;});});document.querySelectorAll('#stars button').forEach((star,index,all)=>{star.addEventListener('click',()=>{all.forEach((s,i)=>s.textContent=i<=index?'★':'☆');document.getElementById('tips').classList.remove('hide');});});go('splash');